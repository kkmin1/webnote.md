# 다이어그램 에디터

범용 다이어그램 드로잉 툴. 게임트리, 플로우차트, 개념도 등 SVG 기반 다이어그램을 웹에서 직접 그리고 편집할 수 있습니다.

---

## 실행 방법

### 로컬 서버 (권장)
```bash
# Python 3
python -m http.server 8000

# Node.js (npx)
npx serve .
```
브라우저에서 `http://localhost:8000` 접속

### 단일 파일로 배포
파일을 분리하지 않고 하나의 HTML로 합치면 `file://`로 바로 열 수 있습니다.
빌드 스크립트 없이 단순히 내용을 index.html에 인라인으로 넣으면 됩니다.

---

## 파일 구조

```
diagram-editor/
├── index.html        HTML 구조 (마크업만)
├── style.css         전체 스타일
├── js/
│   ├── state.js      전역 상태 & 데이터 구조 (오브젝트 스키마)
│   ├── render.js     SVG 렌더링 엔진 & 줌/캔버스 크기
│   ├── tools.js      도구 동작, 이벤트, 오른쪽 패널 동기화
│   ├── io.js         파일 저장/불러오기/내보내기
│   └── app.js        초기화, 키보드 단축키, 전역 이벤트
└── README.md
```

**로드 순서**: `state.js` → `render.js` → `tools.js` → `io.js` → `app.js`
(뒤쪽 파일이 앞쪽 파일의 함수/변수를 참조)

---

## 기능 목록

### 도형 도구
| 도구 | 단축키 | 설명 |
|------|--------|------|
| 선택 | `S` | 클릭 선택, 드래그 이동, 핸들로 크기 조절 |
| 텍스트 | `T` | 클릭 위치에 텍스트 입력 |
| 원 | `C` | 중심→반지름 드래그 |
| 타원 | `E` | 대각선 드래그 |
| 사각형 | `R` | 대각선 드래그 |
| 직선 | `L` | 드래그 |
| 화살표 | `A` | 끝에 화살촉 |
| 점선 | `D` | 점선 직선 |
| 점선화살표 | — | 점선 + 화살촉 |

### 편집
- **선택 후 오른쪽 패널**에서 선 색상/굵기/스타일, 채우기, 투명도, 텍스트 속성, 위치/크기를 실시간 수정
- 도형 완성 후 자동 선택 모드 전환 (HWP 방식)
- 더블클릭으로 텍스트 재편집
- 레이어 순서 변경 (앞으로/뒤로)
- Ctrl+C/V 복사/붙여넣기
- 화살표 키로 1px 미세 이동

### 파일
| 포맷 | 용도 |
|------|------|
| `.dged` | 편집 가능한 저장 파일 (JSON). 모든 오브젝트 데이터 보존 |
| `.svg` | 벡터 이미지 내보내기 |
| `.png` | 래스터 이미지 내보내기 (2× 해상도) |
| 이미지 삽입 | PNG/JPG/GIF/WebP → 캔버스에 오브젝트로 삽입 |

### 뷰
- 줌: `+`/`−` 버튼, `Ctrl+휠`, 맞춤/1:1
- 캔버스 크기: 우하단 핸들로 드래그 조절 (기본 1600×1200)
- 사이드바 너비: 가운데 구분선 드래그로 조절

---

## 새 도형 타입 추가 방법

1. **`js/render.js`** — `makeEl()` 함수에 새 타입 분기 추가
2. **`js/state.js`** — `bbox()`, `hitTest()`, `moveObj()`, `copyPos()` 에 새 타입 처리 추가
3. **`js/tools.js`** — `makeNewObj()`, `updateDrawing()`, `isDegenerate()`, `applyHandle()`, `syncProps()` 에 추가
4. **`index.html`** — 왼쪽 패널 도구 그리드에 버튼 추가
5. **`js/app.js`** — `TOOL_KEYS` 에 단축키 추가 (선택)

---

## 기술 스택

- **Vanilla JS** (프레임워크 없음)
- **SVG** 기반 렌더링
- **CSS Flexbox** 레이아웃
- 외부 라이브러리 없음 (CDN 의존 없음)

---

## 키보드 단축키 전체

| 단축키 | 동작 |
|--------|------|
| `Ctrl+Z` | 실행 취소 |
| `Ctrl+Y` | 재실행 |
| `Ctrl+C` | 복사 |
| `Ctrl+V` | 붙여넣기 |
| `Ctrl+A` | 마지막 오브젝트 선택 |
| `Delete` | 선택 오브젝트 삭제 |
| `Escape` | 선택 해제 |
| `↑↓←→` | 1px 이동 |
| `Ctrl+휠` | 줌 인/아웃 |
| `S` / `V` | 선택 도구 |
| `C` | 원 도구 |
| `E` | 타원 도구 |
| `R` | 사각형 도구 |
| `L` | 직선 도구 |
| `A` | 화살표 도구 |
| `T` | 텍스트 도구 |
| `D` | 점선 도구 |
